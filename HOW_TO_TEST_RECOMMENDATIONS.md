# دليل اختبار نظام التوصيات الجديد

## الخطوات للتحقق من أن النظام يعمل

### 1. اختبار قاعدة البيانات
```bash
# تشغيل اختبار قاعدة البيانات
node scripts/test-recommendation-database.js
```

### 2. اختبار API التوصيات
```bash
# بدء الخادم
npm run dev

# في متصفح آخر، اختبر API التوصيات:
# GET /api/recommendations?type=mixed&limit=8
```

### 3. اختبار الواجهة
1. سجل دخول في الموقع
2. اذهب إلى الصفحة الرئيسية
3. ابحث عن قسم "منتجات مقترحة"
4. لاحظ مؤشر المصدر (ذاكرة/قاعدة بيانات/جديد)

### 4. اختبار الاستمرارية عبر الأجهزة
1. سجل دخول على جهاز/متصفح
2. اطلع على التوصيات
3. سجل دخول على جهاز/متصفح آخر
4. تأكد من رؤية نفس التوصيات

## النتائج المتوقعة

✅ **النظام يحفظ التوصيات في قاعدة البيانات**
✅ **العميل يرى نفس التوصيات من أي جهاز**
✅ **التوصيات تتحدث مع كل استخدام جديد**
✅ **مؤشرات المصدر تعمل بشكل صحيح**

## استكشاف الأخطاء

### إذا لم تظهر التوصيات:
1. تأكد من تسجيل الدخول
2. تحقق من وجود منتجات في قاعدة البيانات
3. راجع console المتصفح للأخطاء

### إذا لم تُحفظ التوصيات:
1. تحقق من اتصال قاعدة البيانات
2. تأكد من وجود جدول RecommendationHistory
3. راجع logs الخادم للأخطاء

## الملفات المحدثة

- `src/app/api/recommendations/route.ts` - API التوصيات مع حفظ في قاعدة البيانات
- `src/components/product/SmartRecommendations.tsx` - مكون التوصيات مع مؤشرات المصدر
- `prisma/schema.prisma` - جداول قاعدة البيانات (موجودة مسبقاً)
- `scripts/test-recommendation-database.js` - اختبار النظام

## الخلاصة

النظام الآن يضمن:
- **الاستمرارية**: التوصيات محفوظة ومتاحة عبر الأجهزة
- **الأداء**: تخزين مؤقت متعدد المستويات
- **الذكاء**: توصيات مخصصة بناءً على سلوك العميل
- **الشفافية**: مؤشرات واضحة لمصدر التوصيات
